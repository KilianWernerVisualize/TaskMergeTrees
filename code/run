#!/usr/bin/env bash
set -ex

mkdir build
pushd build

cmake -G Ninja \
      -DCMAKE_BUILD_TYPE=Release \
      ../HPX-Contour-Trees-master/HPX

cmake --build .

./hpxct \
    /data/ctBones.vti \
   	| tee /results/output

ls
cp output_0.vti /results/

popd

